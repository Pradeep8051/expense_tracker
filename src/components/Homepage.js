import React from 'react';
import ExpenseList from './ExpenseList';

const HomePage = () => {
  return (
    <div>
      <h2>Expense List</h2>
      <ExpenseList />
    </div>
  );
};

export default HomePage;
